name: Deploy Portal

on:
  push:
    branches: [main]
    paths:
      - 'apps/portal/**'
      - 'packages/shared/**'
      - 'packages/ui/**'

jobs:
  deploy:
    name: Deploy Customer Portal
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # TODO: Deploy to Cloudflare Pages
      # Use wrangler pages deploy or Cloudflare Pages GitHub integration
      - name: Placeholder
        run: echo "Portal deployment not yet configured"
