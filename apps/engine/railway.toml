[build]
builder = "NIXPACKS"
buildCommand = "pnpm install && pnpm turbo run build --filter=engine"

[deploy]
startCommand = "node apps/engine/dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
